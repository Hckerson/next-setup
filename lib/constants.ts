export const DATE_VALUE_FORMAT = "yyyy-MM-dd";

export const MOTION_DURATION = {
    fast: 0.1,
    slow: 0.2,
} as const;

export const MOTION_SPRING = {
    type: "spring",
    stiffness: 100,
    damping: 30,
} as const;
